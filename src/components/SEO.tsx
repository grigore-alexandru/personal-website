// Deprecated: SEO metadata is now handled via generateMetadata in page.tsx Server Components.
// This stub exists for backward-compatibility during the migration and renders nothing.

import React from 'react';
import { SEOProps } from '../config/seo';

const SEO: React.FC<SEOProps> = () => null;

export default SEO;
